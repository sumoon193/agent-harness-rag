# EnterpriseMind Agent Harness RAG — 分阶段完整开发规划

> **2026-07-13 当前主线：** 项目已从单轮 HR 问答深化为“通用 Agent Runtime + HR Shared Service Reference Application”。原 16 阶段作为 V1 建设历史保留；当前新增阶段 17，聚焦长期 Case、Event Store、执行治理、Context/Memory/Skill、MCP/A2A 和 Safety Eval。GraphRAG 仍不进入主链路。

> 本文档基于当前代码、模块规范与验证合同，
> 以“Agent Harness 治理层 + RAG 证据层”双线架构为核心，
> 制定从基础能力到完整可运行系统的分阶段开发规划。

---

## 项目信息

- **项目名称：** EnterpriseMind Agent Runtime
- **项目定位：** 面向企业制度型长流程的 Agent Runtime；RAG 是证据层，Harness 是执行治理层，HR 是 Reference Application
- **开发环境：** Windows 11 + Docker Compose + Python 3.12
- **技术栈：** Python、FastAPI、LangGraph、LangChain Core、Celery、Redis、PostgreSQL、Milvus、Elasticsearch、Docling / MinerU、Qwen Cloud、RAGAS、Phoenix、OpenTelemetry、Vue 3、Element Plus、TypeScript、Docker Compose
- **Reference Application：** 新员工入职到转正的跨天 Case；报销和请假作为轻量评测场景

## 阶段 17：Agent Runtime 工业化深化（2026-07-13）

已落地：

- append-only Event Store、optimistic version、command idempotency、hash chain、transactional outbox。
- PostgreSQL/SQLAlchemy 与 in-memory async adapters、Case projection/rebuild、run lease/fencing token。
- approval revision/expiry/revoke/subject hash/maker-checker 和执行前重新授权。
- effectively-once SideEffect Ledger、unknown/reconciliation、Durable Timer。
- DocumentVersion、稳定 chunk ID、active-version 检索和 evidence freshness。
- Context Snapshot、episodic memory、Skill 供应链治理。
- PLAN/ACT/OBSERVE/REFLECT/REPAIR、MCP 2025-11-25、本地 A2A read-only peer。
- trajectory Safety Eval、`case_id/run_id/event_id` trace、Runtime metrics。
- Case-centric Vue 运维台和 Artifact Timeline。

详细规范见 `docs/modules/15-AgentRuntime长期Case与协议治理.md`。

---

## 规划优化说明（2026-05-28）

本规划保留 16 个阶段的完整路线，但实际开发建议采用“纯领域闭环优先，基础设施逐步替换”的方式推进，避免一开始被 Docker、Milvus、Elasticsearch、模型 API 和前端同时阻塞。

### 推荐实施顺序

1. **纯后端领域闭环**：先实现 schemas、chunking、in-memory retrieval、Agent Harness、审批恢复、deterministic eval，所有测试不依赖 Docker 和云 API。
2. **API 闭环**：用 FastAPI 暴露上传、Agent Run、审批、评测接口，先接本地 fallback。
3. **基础设施替换**：再把 in-memory adapter 替换为 PostgreSQL、Redis、Milvus、Elasticsearch、MinIO、Celery。
4. **前端控制台**：后端主链路稳定后再建设前端控制台，当前实现采用 Vue 3 + Element Plus，重点呈现 evidence、approval 与 trace 的运行状态。
5. **Runtime 深化**：长期 Case、Event Store、MCP/A2A、安全评测和运维台已实现；GraphRAG / LightRAG 仍不进入主链路。

### 最新技术口径

- Qwen 云端文本向量优先使用 `text-embedding-v4`，重排使用 `qwen3-rerank`；开源 Qwen3-Embedding / Qwen3-Reranker 作为本地或私有化备选。
- Milvus 使用 multi-vector hybrid search 思路，支持 dense / sparse、多字段或多模态检索；向量维度应由 embedding provider 配置决定，不在 schema 中写死。
- Docling 作为主文档解析 adapter，覆盖 PDF、DOCX、XLSX、PPTX、Markdown、HTML、CSV、图片、音频、视频等输入；MinerU 作为复杂 PDF、扫描件、版面分析增强选项。
- LangGraph 用于长运行、有状态、可中断、可恢复的 agent orchestration，重点使用 persistence、interrupt/resume、streaming 和 human-in-the-loop。
- MCP 已实现本地 2025-11-25 HTTP/JSON-RPC reference server；远程生态与官方 SDK 完整 transport/session 仍不作为运行前提。
- OWASP LLM / GenAI 风险表达使用 2025 口径，重点关注 Prompt Injection、Sensitive Information Disclosure、System Prompt Leakage、Vector and Embedding Weaknesses、Excessive Agency、Unbounded Consumption。

### 模块开发规范文档

每个模块的详细开发规范已拆分到 `docs/modules/`：

- `00-模块规范总览.md`
- `01-基础设施与项目骨架.md`
- `02-数据模型与Schema.md`
- `03-文档上传与异步入库.md`
- `04-文档解析与分块.md`
- `05-索引与混合检索.md`
- `06-AgentHarness核心.md`
- `07-LangGraph工作流.md`
- `08-GroundedAnswer与评测.md`
- `09-ACL与安全.md`
- `10-可观测性与Trace.md`
- `11-前端控制台.md`
- `12-V2扩展GraphRAG与MCP.md`
- `13-API与服务编排.md`
- `14-测试与质量门禁.md`

---

## 分阶段规划总览

| 阶段 | 名称 | 核心能力 | 复杂度 | 预估工时 |
|------|------|----------|--------|----------|
| 1 | 项目脚手架与基础设施 | Docker Compose 全栈、健康检查 | 中 | 3-4h |
| 2 | 数据模型与共享 Schemas | Pydantic Schemas、SQLAlchemy Models、Alembic | 低 | 2-3h |
| 3 | 文档上传与对象存储 | 文件上传、MinIO 存储、文档元数据 | 低 | 2-3h |
| 4 | 异步文档入库流水线 | Celery 任务链、进度追踪、失败重试 | 高 | 4-6h |
| 5 | 复杂文档结构化解析 | Docling 主解析、MinerU 增强、Parser Registry | 中 | 3-4h |
| 6 | 多粒度 Chunking 与 Contextual Retrieval | 结构化+语义+Parent-Child+上下文前缀 | 高 | 5-7h |
| 7 | Embedding、Milvus 与 Elasticsearch 索引 | Dense+Sparse 向量、BM25、中文分词 | 中高 | 3-5h |
| 8 | 混合检索与 Reranker | Dense+Sparse+RRF+Reranker 精排 | 中高 | 4-5h |
| 9 | Agent Harness 核心：Run 生命周期 | AgentRun 状态机、Step 记录、Tool Registry | 高 | 5-7h |
| 10 | LangGraph 编排与 Human-in-the-Loop | 状态图、中断/恢复、审批流、checkpoint | 高 | 5-7h |
| 11 | Agentic RAG 完整链路 | 意图识别→改写→检索→评分→计划→执行→校验 | 高 | 4-6h |
| 12 | Grounded Answer 与评测 | 证据约束、引用生成、RAGAS 评测 | 中高 | 4-5h |
| 13 | ACL 权限与安全防护 | 文档级 ACL、OWASP LLM、Prompt Injection | 中 | 3-4h |
| 14 | 可观测性与 Trace | Phoenix、OpenTelemetry、Span 树 | 中 | 3-4h |
| 15 | 前端控制台 | 文档管理、Agent 控制台、审批面板、Trace | 中高 | 5-7h |
| 16 | V2 增强：GraphRAG / MCP | LightRAG 知识图谱、MCP 接口 | 中 | 4-5h |

---

## 阶段 1：项目脚手架与基础设施

### 目标

建立项目骨架、Docker Compose 全栈基础设施，以及健康检查端点证明所有服务可互相通信。

### 对应能力

- Docker Compose 本地全栈部署

### 要做的事

#### 1.1 创建项目目录结构

```
enterprisemind-rag/
├── docker-compose.yml
├── docker-compose.app.yml          # 应用层服务（API、Worker、前端）
├── .env                            # 环境变量
├── .env.example                    # 模板（不含密钥）
├── .gitignore
├── pyproject.toml                  # 项目元数据 + 依赖（使用 pip 或 uv）
├── alembic.ini
├── alembic/
│   ├── env.py
│   └── versions/
├── scripts/
│   ├── wait-for-it.sh              # 服务就绪检测
│   └── init_db.sh                  # Alembic 迁移启动脚本
├── app/
│   ├── __init__.py
│   ├── main.py                     # FastAPI 入口
│   ├── config.py                   # Pydantic Settings（读 .env）
│   ├── dependencies.py             # FastAPI 依赖注入
│   └── core/                       # 基础设施连接层
│       ├── __init__.py
│       ├── database.py             # SQLAlchemy async engine + session
│       ├── redis_client.py         # Redis 连接
│       ├── milvus_client.py        # Milvus 连接
│       ├── es_client.py            # Elasticsearch 连接
│       └── storage.py              # MinIO 客户端
└── tests/
    ├── __init__.py
    ├── conftest.py                 # Pytest fixtures
    └── test_health.py
```

#### 1.2 Docker Compose 基础设施服务

| 服务 | 镜像 | 端口 | 用途 |
|------|------|------|------|
| postgres | postgres:16-alpine | 5432 | 元数据、用户、ACL、文档注册 |
| redis | redis:7-alpine | 6379 | Celery broker、缓存 |
| milvus-standalone | milvusdb/milvus:稳定版本 | 19530 | Dense + Sparse / multi-vector 向量存储 |
| etcd | quay.io/coreos/etcd:v3.5.18 | 2379 | Milvus 元数据后端 |
| minio | minio/minio:latest | 9000, 9001 | 原始文件对象存储 |
| elasticsearch | elasticsearch:8.x 稳定版本 | 9200 | BM25 全文索引 |

#### 1.3 pyproject.toml 核心依赖

```toml
[project]
name = "enterprisemind-rag"
version = "0.1.0"
requires-python = ">=3.12"

dependencies = [
    "fastapi>=0.115",
    "uvicorn[standard]>=0.34",
    "pydantic-settings>=2.7",
    "python-dotenv>=1.0",
    "python-multipart>=0.0.18",
    "httpx>=0.28",
    # Database
    "sqlalchemy[asyncio]>=2.0",
    "asyncpg>=0.30",
    "alembic>=1.14",
    # Cache / Queue
    "redis[hiredis]>=5.2",
    # Vector / Search
    "pymilvus[model]>=2.5",
    "elasticsearch[async]>=8.13",
    # Object Storage
    "minio>=7.2",
]
```

#### 1.4 config.py 设计要点

使用 `pydantic-settings.BaseSettings` 从 `.env` 加载所有配置：

- `POSTGRES_URL`、`REDIS_URL`、`MILVUS_HOST`、`MILVUS_PORT`
- `ES_URL`、`MINIO_ENDPOINT`、`MINIO_ACCESS_KEY`、`MINIO_SECRET_KEY`、`MINIO_BUCKET`
- `QWEN_API_KEY`、`QWEN_EMBEDDING_MODEL`、`QWEN_RERANK_MODEL`
- `LLM_BASE_URL`、`LLM_MODEL`、`LLM_API_KEY`
- `ENVIRONMENT`（dev / test / prod）

#### 1.5 健康检查端点

`GET /health` 返回每个服务的状态和延迟：

```json
{
  "status": "healthy",
  "services": {
    "postgres": {"status": "up", "latency_ms": 2},
    "redis": {"status": "up", "latency_ms": 1},
    "milvus": {"status": "up", "latency_ms": 5},
    "elasticsearch": {"status": "up", "latency_ms": 3},
    "minio": {"status": "up", "latency_ms": 4}
  }
}
```

### 验证方式

1. `docker compose up -d` 启动所有基础设施容器无报错
2. `pip install -e .` 安装项目和所有依赖
3. `uvicorn app.main:app --reload` 启动 API 服务
4. `curl http://localhost:8000/health` 返回所有服务 `"up"`
5. `pytest tests/test_health.py` 通过

---

## 阶段 2：数据模型与共享 Schemas

### 目标

定义整个系统的核心数据结构，为后续所有阶段提供类型基础。

### 对应能力

- Agent Run 生命周期管理（状态机）
- Tool Registry 与权限分级
- 文档级 ACL 权限隔离
- 评测闭环

### 要做的事

#### 2.1 Pydantic Schemas（app/schemas/）

```
app/schemas/
├── __init__.py
├── document.py         # DocumentCreate, DocumentResponse, DocumentStatus
├── task.py             # IngestionTask, TaskStatus
├── chunk.py            # Chunk, ChunkWithParent, EvidenceBundle
├── citation.py         # Citation, CitationSource
├── retrieval.py        # RetrievalResult, RetrievalRequest
├── chat.py             # ChatRequest, AnswerResponse
├── agent.py            # AgentRun, AgentStep, AgentPlan, RunStatus 枚举
├── tool.py             # ToolDefinition, ToolCall, ToolResult, ToolApproval
├── approval.py         # ApprovalRequest, ApprovalDecision
├── eval.py             # EvalCase, EvalResult, EvalRun
└── user.py             # UserContext, PermissionFilter
```

#### 2.2 关键枚举定义

```python
class RunStatus(str, Enum):
    CREATED = "created"
    RUNNING = "running"
    RETRIEVING_EVIDENCE = "retrieving_evidence"
    PLANNING = "planning"
    AWAITING_APPROVAL = "awaiting_approval"
    RESUMED = "resumed"
    COMPLETED = "completed"
    FAILED = "failed"
    CANCELLED = "cancelled"

class DocumentStatus(str, Enum):
    PENDING = "pending"
    PARSING = "parsing"
    CHUNKING = "chunking"
    INDEXING = "indexing"
    READY = "ready"
    FAILED = "failed"

class ToolRiskLevel(str, Enum):
    READ = "read"       # 只读，自动执行
    WRITE = "write"     # 写入型，必须审批
    ADMIN = "admin"     # 管理级，必须审批

class Visibility(str, Enum):
    PUBLIC = "public"
    DEPARTMENT = "department"
    PRIVATE = "private"
    CONFIDENTIAL = "confidential"
```

#### 2.3 SQLAlchemy Models（app/models/）

```
app/models/
├── __init__.py
├── base.py             # DeclarativeBase
├── document.py         # Document（含 ACL 字段）
├── chunk.py            # Chunk（含 parent_id, context_prefix, heading_path）
├── agent_run.py        # AgentRun（含状态机、thread_id）
├── agent_step.py       # AgentStep（含节点名、输入输出、证据、token）
├── tool_call.py        # ToolCall（含工具名、参数、结果、审批状态）
├── approval.py         # Approval（含决策、决策人、时间）
├── eval_case.py        # EvalCase, EvalRun
└── user.py             # User, Role, Permission
```

#### 2.4 Alembic 迁移

- `alembic init` 初始化迁移目录
- 创建初始迁移：所有表
- `alembic upgrade head` 验证表创建成功

### 验证方式

1. `alembic upgrade head` 创建所有表成功
2. `pytest tests/test_schemas.py` 验证 Pydantic schema 校验
3. 数据库中能看到所有表结构

---

## 阶段 3：文档上传与对象存储

### 目标

接受文档上传，存入 MinIO，在 PostgreSQL 注册元数据。不做解析，只做文件入库和追踪。

### 对应能力

- 多格式复杂文档入库（支持 PDF、DOCX、XLSX、PPTX、Markdown、HTML、CSV、图片）
- 文档级 ACL metadata 写入

### 要做的事

#### 3.1 API 端点

| 方法 | 路径 | 说明 |
|------|------|------|
| POST | `/api/v1/documents/upload` | 上传文件，返回 document_id |
| GET | `/api/v1/documents` | 列出文档（分页、支持 ACL 过滤） |
| GET | `/api/v1/documents/{id}` | 获取文档元数据 |
| DELETE | `/api/v1/documents/{id}` | 软删除文档 |

#### 3.2 上传流程

1. 接收 `UploadFile`，校验文件类型（允许列表：pdf, docx, xlsx, pptx, md, html, csv, png, jpg）
2. 校验文件大小（默认最大 100 MB）
3. 生成 UUID，构造 MinIO 对象键：`{tenant}/{year}/{month}/{uuid}/{filename}`
4. 上传到 MinIO bucket
5. 在 PostgreSQL 插入 document 记录，status = `PENDING`
6. 写入 ACL metadata：tenant_id、department_id、visibility

#### 3.3 新增文件

```
app/api/v1/
├── documents.py            # 上传、列表、详情、删除端点
app/services/
├── document_service.py     # 文档 CRUD 业务逻辑
app/schemas/
└── document.py             # (更新) 增加完整字段
tests/
├── test_documents.py
├── fixtures/
│   ├── sample.pdf
│   └── sample.docx
```

### 验证方式

1. `POST /api/v1/documents/upload` 上传 PDF → 201，返回 document_id 和 status "pending"
2. MinIO Console `http://localhost:9001` 可见文件
3. `GET /api/v1/documents` 列出已上传文档
4. 上传 .exe 文件 → 400 校验失败
5. `pytest tests/test_documents.py` 通过

---

## 阶段 4：异步文档入库流水线

### 目标

解耦文档处理与上传请求。上传立即返回 task ID，后台 Worker 异步执行解析→切分→向量化→索引，支持进度追踪和失败重试。

### 对应能力

- 异步文档入库流水线（FastAPI + Celery + Redis）
- 大文件后台处理、任务进度追踪、失败重试、状态回查
- 单阶段重试，不必整个流程重来

### 要做的事

#### 4.1 Celery 配置

```
app/worker/
├── __init__.py
├── celery_app.py           # Celery 应用配置
└── tasks/
    ├── __init__.py
    ├── ingest.py            # 任务链编排：parse → chunk → embed → index
    ├── parse_task.py        # 第 1 步：解析文档
    ├── chunk_task.py        # 第 2 步：切分文档（先用简单 fixed-size）
    ├── embed_task.py        # 第 3 步：生成 embedding
    └── index_task.py        # 第 4 步：写入 Milvus + Elasticsearch
```

#### 4.2 Celery 配置要点

- Broker: `redis://redis:6379/0`
- Backend: `redis://redis:6379/1`
- `task_track_started=True`、`task_acks_late=True`
- 每个 task 开始和完成时更新 PostgreSQL 中 document 的 status

#### 4.3 任务链设计

```python
# ingest.py
chain(
    parse_task.s(document_id),
    chunk_task.s(),
    embed_task.s(),
    index_task.s()
).apply_async()
```

状态流转：`PENDING → PARSING → CHUNKING → INDEXING → READY`（失败 → `FAILED` + error_message）

#### 4.4 简单 Parser 实现（阶段 5 升级为 Docling/MinerU）

- `pdf_parser.py`：PyMuPDF 逐页提取文本
- `docx_parser.py`：python-docx 提取段落和标题层级
- `plain_parser.py`：.txt / .md 直接读取

#### 4.5 简单 Chunker 实现（阶段 6 升级为多粒度）

- `fixed_size.py`：~500 tokens/chunk，~100 tokens 重叠

#### 4.6 任务状态查询端点

| 方法 | 路径 | 说明 |
|------|------|------|
| GET | `/api/v1/tasks/{document_id}` | 查询入库任务当前状态和进度 |

#### 4.7 Docker Compose 应用层服务

```yaml
celery-worker:
  build: .
  command: celery -A app.worker.celery_app worker --loglevel=info --concurrency=2
  depends_on: [redis, postgres]

celery-flower:
  image: mher/flower:latest
  ports: ["5555:5555"]
  command: celery --broker=redis://redis:6379/0 flower
```

#### 4.8 新增依赖

```toml
"celery[redis]>=5.4",
"pymupdf>=1.25",
"python-docx>=1.1",
```

### 验证方式

1. 上传 PDF → 返回 201 和 document_id
2. `GET /api/v1/tasks/{id}` 显示状态流转：PENDING → PARSING → CHUNKING → INDEXING → READY
3. Milvus 中有向量数据
4. Elasticsearch 中有文本数据
5. Flower `http://localhost:5555` 显示完成的任务
6. 上传损坏文件 → status = FAILED，有 error_message
7. `pytest tests/test_ingestion.py` 通过

---

## 阶段 5：复杂文档结构化解析

### 目标

用 Docling 替换简单 Parser，支持高质量文档结构提取，正确处理表格、标题、页码、图片等。

### 对应能力

- 复杂文档结构化解析（Docling 主解析、MinerU 增强）
- 保留标题层级、页码、表格结构、图片说明、章节关系、阅读顺序
- Parser Registry 自动路由

### 要做的事

#### 5.1 Parser 接口与 Registry

```
app/services/parser/
├── __init__.py
├── base.py             # 抽象 Parser 接口，输出 ParsedDocument
├── registry.py         # MIME Type → Parser 映射
├── docling_parser.py   # Docling 主解析器（PDF, DOCX, XLSX, PPTX, MD, HTML, CSV）
├── mineru_parser.py    # MinerU 增强解析器（复杂 PDF、扫描件、版面分析）
├── image_parser.py     # 图片 OCR 解析
└── plain_parser.py     # 纯文本直通
```

#### 5.2 Docling 集成

Docling 将多种格式统一转换为 Markdown / JSON / Text：

```python
from docling.document_converter import DocumentConverter

async def parse(file_path: str) -> ParsedDocument:
    converter = DocumentConverter()
    result = converter.convert(file_path)
    md_content = result.document.export_to_markdown()
    return ParsedDocument(
        content=md_content,
        pages=extract_pages(result),
        tables=extract_tables(result),
        headings=extract_heading_hierarchy(result),
        metadata=extract_metadata(result),
    )
```

#### 5.3 结构化输出 Schema

```python
class ParsedPage(BaseModel):
    page_number: int
    text: str
    tables: list[Table]
    images: list[ImageRef]

class Table(BaseModel):
    page_number: int
    rows: list[list[str]]
    caption: str
    markdown: str           # 表格的 Markdown 表示

class ParsedDocument(BaseModel):
    document_id: str
    content: str            # 完整 Markdown
    pages: list[ParsedPage]
    heading_hierarchy: list[HeadingNode]
    total_pages: int
    parser_used: str
    acl_metadata: dict
```

#### 5.4 新增依赖

```toml
"docling>=2.0",
"magic-pdf[full]>=1.3",    # MinerU 可选
"python-pptx>=1.0",
"openpyxl>=3.1",
```

#### 5.5 更新阶段 4 的 parse_task

- 使用 `registry.get_parser(mime_type)` 替代 if/else
- 输出结构化 `ParsedDocument` 传递给后续切分步骤

### 验证方式

1. 上传包含表格的 PDF → Docling 正确提取表格为 Markdown
2. 上传 DOCX → 标题层级保留
3. 上传 XLSX → 行数据转换为可读文本
4. 对比：同一份 PDF 用简单 Parser vs Docling → Docling 版本结构更完整
5. `pytest tests/test_parsers.py` 通过

---

## 阶段 6：多粒度 Chunking 与 Contextual Retrieval

### 目标

替换简单 fixed-size 切分为"结构化 + 语义 + Parent-Child + Contextual Retrieval"组合策略。**这是对检索质量提升最大的单一阶段。**

### 对应能力

- 多粒度 Chunking（结构化 + 语义 + Parent-Child + 表格保护）
- Contextual Retrieval（50-100 token 上下文前缀）
- Parent-Child 检索机制（child 精确命中，parent 补充上下文）

### 要做的事

#### 6.1 Chunker 模块

```
app/services/chunker/
├── __init__.py
├── base.py                 # 抽象 Chunker 接口
├── structural_chunker.py   # 按标题、章节、表格、列表切 parent chunk
├── semantic_chunker.py     # 按语义完整性切 child chunk（200-400 tokens）
├── parent_child.py         # 生成 parent-child 关系
├── table_protector.py      # 表格内容按行组、表头一起保存
├── hybrid_chunker.py       # 编排：structural → semantic → parent-child
└── context_enricher.py     # 生成 Contextual Retrieval 前缀
```

#### 6.2 结构化分块逻辑

- 按 `ParsedDocument.heading_hierarchy` 切分为 parent chunk
- 每个 parent chunk 包含：标题路径、页码范围、完整文本
- 表格作为原子单元，不跨表切割
- 列表在可能的情况下保持完整

#### 6.3 语义分块逻辑

- 在每个 parent chunk 内，按句子级语义连贯性生成 child chunk
- 目标大小：200-400 tokens
- 确保不在句子中间断开
- 使用滑动窗口连贯性检测

#### 6.4 Parent-Child 关系

```python
class Chunk(BaseModel):
    id: str
    document_id: str
    chunk_text: str              # 原始文本
    context_prefix: str          # 生成的上下文前缀
    full_text: str               # context_prefix + chunk_text（用于 embedding）
    parent_id: str | None        # parent chunk ID（parent 本身为 None）
    chunk_type: str              # "parent" 或 "child"
    heading_path: str            # 如 "HR制度 > 第三章 > 3.2 请假流程"
    page_numbers: list[int]
    token_count: int
    acl_metadata: dict           # 从文档继承的权限信息
```

#### 6.5 Contextual Retrieval 前缀生成

```python
async def generate_context(chunk: str, document_name: str, heading_path: str) -> str:
    prompt = f"""
    以下是企业文档的一个片段。请用50-100个字描述这个片段的上下文，
    包括它来自哪份文档、哪个章节、讨论什么主题。

    文档: {document_name}
    章节路径: {heading_path}
    片段内容: {chunk}

    只输出上下文描述，不要重复片段内容。
    """
    context = await llm.generate(prompt)
    return context
    # 例: "本片段来自《员工考勤制度》第三章'请假流程'，
    #       说明了事假、病假和年假的申请条件和审批层级。"
```

#### 6.6 更新 Milvus Schema

新增字段：`parent_id`、`chunk_type`、`heading_path`、`context_prefix`

#### 6.7 更新 chunk_task 和 embed_task

- chunk_task 使用 hybrid_chunker 替代 fixed_size
- chunking 完成后调用 context_enricher 为每个 chunk 生成前缀
- embed_task 对 `full_text`（context + chunk）做 embedding

#### 6.8 新增依赖

```toml
"tiktoken>=0.8",
```

### 验证方式

1. 入库多章节制度文档 → Milvus 中 parent chunk 500-1000 tokens，child chunk 200-400 tokens
2. 每个 child chunk 有非空 `context_prefix`，提到文档名和章节
3. 检索返回 child chunk，答案生成用 parent chunk 补充上下文
4. 表格未被跨 chunk 切割
5. 对比：有/无 Contextual Prefix 的 top-10 召回命中率 → 有 prefix 时提升 15-25%
6. `pytest tests/test_chunkers.py` + `tests/test_contextual.py` 通过

---

## 阶段 7：Embedding、Milvus 与 Elasticsearch 索引

### 目标

建立完整的向量化和索引体系，支持 Dense + Sparse 多向量检索。

### 对应能力

- Embedding 与 Reranker 策略（云端 text-embedding-v4、本地 Qwen3-Embedding）
- Milvus multi-vector hybrid search
- Elasticsearch BM25 + IK 中文分词

### 要做的事

#### 7.1 Embedding 服务

```
app/services/embedding/
├── __init__.py
├── base.py                 # 抽象 Embedder 接口
├── qwen_cloud_embedder.py  # Qwen Cloud text-embedding-v4
├── local_embedder.py       # SentenceTransformer 本地 Qwen3-Embedding
└── factory.py              # 根据配置选择 embedding 实现
```

#### 7.2 Milvus 索引设计

```python
# document_chunks collection
fields = [
    FieldSchema("id", VARCHAR, is_primary=True, max_length=64),
    FieldSchema("document_id", VARCHAR, max_length=64),
    FieldSchema("chunk_text", VARCHAR, max_length=8192),
    FieldSchema("context_prefix", VARCHAR, max_length=1024),
    FieldSchema("full_text", VARCHAR, max_length=9216),
    FieldSchema("parent_id", VARCHAR, max_length=64),
    FieldSchema("chunk_type", VARCHAR, max_length=16),
    FieldSchema("heading_path", VARCHAR, max_length=512),
    FieldSchema("page_numbers", JSON),
    FieldSchema("tenant_id", VARCHAR, max_length=64),
    FieldSchema("department_id", VARCHAR, max_length=64),
    FieldSchema("visibility", VARCHAR, max_length=32),
    FieldSchema("dense_vector", FLOAT_VECTOR, dim=settings.EMBEDDING_DIM),
    FieldSchema("sparse_vector", SPARSE_FLOAT_VECTOR),
]
# HNSW index on dense_vector (COSINE)
# SPARSE_INVERTED_INDEX on sparse_vector
```

#### 7.3 Elasticsearch 索引设计

```json
{
  "mappings": {
    "properties": {
      "text": {"type": "text", "analyzer": "ik_max_word"},
      "context_prefix": {"type": "text", "analyzer": "ik_max_word"},
      "document_id": {"type": "keyword"},
      "parent_id": {"type": "keyword"},
      "heading_path": {"type": "keyword"},
      "page_numbers": {"type": "integer"},
      "tenant_id": {"type": "keyword"},
      "department_id": {"type": "keyword"},
      "visibility": {"type": "keyword"}
    }
  }
}
```

#### 7.4 Indexer 模块

```
app/services/indexer/
├── __init__.py
├── base.py                 # 抽象 Indexer 接口
├── milvus_indexer.py       # 写入 Dense + Sparse 向量
├── es_indexer.py           # 写入 BM25 索引
└── hybrid_indexer.py       # 同时写入两者
```

### 验证方式

1. 入库文档后，Milvus `document_chunks` collection 有向量数据
2. `GET http://localhost:9201/document_chunks/_count` 返回正确数量
3. 检查 Milvus 中 dense_vector 和 sparse_vector 字段都有值
4. `pytest tests/test_indexing.py` 通过

---

## 阶段 8：混合检索与 Reranker

### 目标

实现 Dense + Sparse + RRF + Reranker 的完整检索链路，同时支持语义查询和精确查询。

### 对应能力

- Dense + Sparse 混合检索 + RRF 融合
- Reranker 二阶段重排（Qwen Cloud qwen3-rerank / 本地 BGE-Reranker）
- Parent-Child 检索机制（child 检索，parent 补充上下文）

### 要做的事

#### 8.1 Retrieval 模块

```
app/services/retrieval/
├── __init__.py
├── base.py                 # 抽象 Retriever 接口
├── vector_retriever.py     # Dense 向量检索（Milvus）
├── bm25_retriever.py       # Sparse / BM25 检索（Elasticsearch）
├── hybrid_retriever.py     # Dense + Sparse + RRF 融合
├── reranker.py             # Reranker 精排
└── evidence_builder.py     # 构建 EvidenceBundle（含 parent context 补充）
```

#### 8.2 检索链路

```python
async def hybrid_search(query: str, user: UserContext, top_k: int = 5):
    # 1. 生成 ACL 过滤条件
    acl_filter = build_acl_filter(user)

    # 2. Dense 向量检索（top 20）
    dense_results = await vector_retriever.search(query, top_k=20, filter=acl_filter)

    # 3. BM25 检索（top 20）
    bm25_results = await bm25_retriever.search(query, top_k=20, filter=acl_filter)

    # 4. RRF 融合
    fused = reciprocal_rank_fusion([dense_results, bm25_results], k=60)

    # 5. 补充 parent chunk 上下文
    enriched = await evidence_builder.enrich_with_parent(fused[:top_k * 3])

    # 6. Reranker 精排
    reranked = await reranker.rerank(query, enriched, top_k=top_k)

    return reranked
```

#### 8.3 RRF 实现

```python
def reciprocal_rank_fusion(results_lists: list[list], k: int = 60) -> list:
    scores = {}
    for results in results_lists:
        for rank, result in enumerate(results):
            doc_id = result.id
            scores[doc_id] = scores.get(doc_id, 0) + 1 / (k + rank + 1)
    return sorted(scores.items(), key=lambda x: x[1], reverse=True)
```

#### 8.4 Reranker 实现

```python
# 云端优先
async def rerank_qwen_cloud(query: str, documents: list[str], top_k: int = 5):
    # 使用 Qwen Cloud qwen3-rerank API
    ...

# 本地备选
async def rerank_local(query: str, documents: list[str], top_k: int = 5):
    from FlagEmbedding import FlagReranker
    reranker = FlagReranker("BAAI/bge-reranker-v2-m3", use_fp16=True)
    scores = reranker.compute_score([[query, doc] for doc in documents])
    ...
```

#### 8.5 新增依赖

```toml
"FlagEmbedding>=1.3",   # 本地 Reranker
"openai>=1.60",         # OpenAI-compatible client
```

### 验证方式

1. 入库含编号"HR-2026-04"的文档 → 查询"HR-2026-04 第12条" → BM25 路径命中精确条款
2. 查询"转正流程怎么走" → 向量路径命中语义相关内容
3. 混合查询两种场景都能正确召回
4. Reranker 将最相关片段排到 top-1
5. 对比：纯向量 recall@10 vs 混合 recall@10 → 混合高 10-20%
6. `pytest tests/test_hybrid_retrieval.py` 通过

---

## 阶段 9：Agent Harness 核心 — Run 生命周期与 Tool Registry

### 目标

实现 Agent Harness 治理层的核心：AgentRun 状态机、Step 记录、Tool Registry、工具权限分级。**这是区别于普通 RAG Chatbot 的核心阶段。**

### 对应能力

- Agent Run 生命周期管理
- Plan-then-Execute 执行策略
- Tool Registry 与权限分级
- 审计日志与 Trace

### 要做的事

#### 9.1 Agent Run 状态机

```
app/services/agent/
├── __init__.py
├── run_manager.py          # AgentRun 创建、状态更新、查询
├── step_manager.py         # AgentStep 记录
├── tool_registry.py        # Tool 注册与查询
├── tool_executor.py        # 工具执行（含沙箱、超时、幂等）
└── plan_manager.py         # Plan 生成与管理
```

#### 9.2 AgentRun 生命周期

```python
class RunManager:
    async def create_run(self, user_id: str, query: str) -> AgentRun:
        """创建新的 Agent Run"""
        run = AgentRun(
            id=uuid4(),
            user_id=user_id,
            original_query=query,
            status=RunStatus.CREATED,
            thread_id=str(uuid4()),  # LangGraph thread
        )
        await self.repo.save(run)
        return run

    async def update_status(self, run_id: str, status: RunStatus):
        """更新 Run 状态"""
        ...

    async def add_step(self, run_id: str, step: AgentStep):
        """记录执行步骤"""
        ...
```

#### 9.3 Tool Registry 设计

```python
class ToolRegistry:
    def __init__(self):
        self._tools: dict[str, ToolDefinition] = {}

    def register(self, tool: ToolDefinition):
        self._tools[tool.name] = tool

    def get(self, name: str) -> ToolDefinition:
        return self._tools[name]

    def list_readable(self) -> list[ToolDefinition]:
        """列出只读工具（可自动执行）"""
        return [t for t in self._tools.values() if t.risk_level == ToolRiskLevel.READ]

    def list_writable(self) -> list[ToolDefinition]:
        """列出写入型工具（需要审批）"""
        return [t for t in self._tools.values() if t.risk_level in (ToolRiskLevel.WRITE, ToolRiskLevel.ADMIN)]
```

#### 9.4 V1 工具实现（模拟工具）

```
app/services/tools/
├── __init__.py
├── base.py                 # 抽象 Tool 接口
├── policy_search.py        # policy_search：检索制度证据，只读
├── get_user_profile.py     # get_user_profile：读取用户信息，只读
├── generate_checklist.py   # generate_hr_checklist：生成办理清单，只读
├── ask_clarification.py    # ask_clarification：追问用户，只读
└── create_mock_ticket.py   # create_mock_hr_ticket：创建模拟工单，写入型
```

#### 9.5 工具定义示例

```python
ToolDefinition(
    name="create_mock_hr_ticket",
    description="创建模拟 HR 工单",
    permission_scope="hr.ticket.write",
    risk_level=ToolRiskLevel.WRITE,
    requires_approval=True,
    timeout_seconds=10,
    idempotent=True,
    parameters_schema={
        "type": "object",
        "properties": {
            "title": {"type": "string"},
            "assignee": {"type": "string"},
            "priority": {"type": "string", "enum": ["low", "medium", "high"]},
            "evidence_ids": {"type": "array", "items": {"type": "string"}},
        },
        "required": ["title", "assignee"],
    },
)
```

### 验证方式

1. 创建 AgentRun → status 为 CREATED
2. 添加 AgentStep → step 记录包含输入输出和时间戳
3. Tool Registry 能列出所有注册工具
4. 只读工具和写入型工具正确分类
5. 模拟工具执行返回预期结果
6. `pytest tests/test_agent_run.py` + `tests/test_tool_registry.py` 通过

---

## 阶段 10：LangGraph 编排与 Human-in-the-Loop

### 目标

使用 LangGraph 构建有状态、可中断、可恢复的 Agent 执行图，实现工具审批的 interrupt/resume 机制。

### 对应能力

- LangGraph 有状态编排（durable execution、streaming、human-in-the-loop、persistence）
- Human-in-the-Loop 工具审批（Approve / Edit / Reject）
- checkpoint/resume 机制

### 要做的事

#### 10.1 LangGraph 状态定义

```python
class AgentState(TypedDict):
    original_query: str
    rewritten_queries: list[str]
    intent: str                      # factual, procedural, comparison, multi_hop, out_of_scope
    retrieved_chunks: list[dict]
    evidence_bundle: list[dict]      # 评分后的相关证据
    evidence_sufficient: bool
    retrieval_attempts: int
    plan: dict                       # 结构化执行计划
    current_step_index: int
    tool_calls: list[dict]
    approval_pending: dict | None    # 等待审批的工具调用
    answer: str
    citations: list[dict]
    factcheck_result: str            # PASS, FAIL, UNCERTAIN
    run_id: str
    user_id: str
    permission_filter: dict
```

#### 10.2 Graph 节点设计

```
app/services/agent/
├── graph.py                # LangGraph StateGraph 定义
├── state.py                # AgentState 定义
├── prompts.py              # 所有 Agent Prompt 模板
└── nodes/
    ├── __init__.py
    ├── intent_node.py      # 意图识别
    ├── rewrite_node.py     # 查询改写
    ├── retrieve_node.py    # 调用 hybrid retriever
    ├── grade_node.py       # 证据评分
    ├── evidence_node.py    # 证据充分性判断
    ├── plan_node.py        # 生成执行计划
    ├── execute_node.py     # 执行当前步骤
    ├── approval_node.py    # 工具审批（interrupt 点）
    ├── generate_node.py    # 生成答案
    ├── factcheck_node.py   # 事实校验
    └── refusal_node.py     # 拒答兜底
```

#### 10.3 Graph 连线

```python
graph = StateGraph(AgentState)

graph.add_node("intent", intent_node)
graph.add_node("rewrite", rewrite_node)
graph.add_node("retrieve", retrieve_node)
graph.add_node("grade", grade_node)
graph.add_node("evidence_check", evidence_node)
graph.add_node("plan", plan_node)
graph.add_node("execute", execute_node)
graph.add_node("approval", approval_node)
graph.add_node("generate", generate_node)
graph.add_node("factcheck", factcheck_node)
graph.add_node("refuse", refusal_node)

graph.set_entry_point("intent")

# 意图路由
graph.add_conditional_edges("intent", route_by_intent, {
    "factual": "rewrite",
    "procedural": "rewrite",
    "comparison": "rewrite",
    "multi_hop": "rewrite",
    "out_of_scope": "refuse",
})
graph.add_edge("rewrite", "retrieve")
graph.add_edge("retrieve", "grade")
graph.add_edge("grade", "evidence_check")

# 证据充分性路由
graph.add_conditional_edges("evidence_check", route_by_evidence, {
    "sufficient": "plan",
    "insufficient": "rewrite",    # 改写查询重试
    "exhausted": "refuse",        # 达到最大重试次数
})

# 计划执行
graph.add_edge("plan", "execute")

# 执行路由
graph.add_conditional_edges("execute", route_by_execution, {
    "needs_approval": "approval",  # 写入工具，需要审批
    "step_complete": "execute",    # 继续执行下一步
    "all_complete": "generate",
    "failed": "refuse",
})

# 审批路由
graph.add_conditional_edges("approval", route_by_approval, {
    "approved": "execute",         # 从 checkpoint 恢复执行
    "edited": "execute",           # 用户修改了参数，继续执行
    "rejected": "generate",        # 跳过此工具，基于已有证据生成答案
})

graph.add_edge("generate", "factcheck")

# 事实校验路由
graph.add_conditional_edges("factcheck", route_by_factcheck, {
    "pass": END,
    "fail": "refuse",
    "uncertain": END,              # 带警告返回
})

graph.add_edge("refuse", END)
```

#### 10.4 interrupt/resume 机制

```python
# 当写入型工具触发时
async def approval_node(state: AgentState):
    tool_call = state["approval_pending"]
    # 使用 LangGraph interrupt 暂停执行
    human_decision = interrupt({
        "type": "approval_required",
        "tool_name": tool_call["name"],
        "parameters": tool_call["parameters"],
        "expected_effect": tool_call["description"],
        "evidence": state["evidence_bundle"][:3],
        "risk_level": "write",
        "options": ["approve", "edit", "reject"],
    })
    return {"approval_decision": human_decision}

# 用户通过 API 提交审批后恢复
async def resume_run(run_id: str, decision: str):
    thread = {"configurable": {"thread_id": run_id}}
    graph.stream(Command(resume=decision), thread)
```

#### 10.5 审批 API 端点

| 方法 | 路径 | 说明 |
|------|------|------|
| POST | `/api/v1/agent/runs` | 创建 Agent Run（开始执行） |
| GET | `/api/v1/agent/runs/{id}` | 获取 Run 状态和详情 |
| POST | `/api/v1/agent/runs/{id}/approve` | 审批工具调用 |
| POST | `/api/v1/agent/runs/{id}/reject` | 拒绝工具调用 |
| POST | `/api/v1/agent/runs/{id}/edit` | 修改工具参数后继续 |
| GET | `/api/v1/agent/runs/{id}/steps` | 获取所有执行步骤 |
| GET | `/api/v1/agent/runs/{id}/stream` | SSE 流式推送步骤更新 |

#### 10.6 新增依赖

```toml
"langgraph>=0.3",
"langchain-core>=0.3",
"langchain-openai>=0.3",
```

### 验证方式

1. 提问事实性问题 → Agent 经过 intent → rewrite → retrieve → grade → plan → execute → generate → factcheck → END
2. 提问触发写入工具 → Agent 在 approval 节点暂停 → API 返回审批请求 → 用户 approve → Agent 从断点恢复
3. 用户 reject 审批 → Agent 跳过工具，基于已有证据生成答案
4. 提问超出范围 → Agent 在 intent 节点直接路由到 refuse
5. 每个 step 记录完整（输入、输出、耗时、token）
6. `pytest tests/test_agent_graph.py` 通过

---

## 阶段 11：Agentic RAG 完整链路集成

### 目标

将 RAG 检索、Agent 编排、工具执行、审批流整合为端到端的完整链路。

### 对应能力

- Agentic RAG 工作流（意图识别→查询改写→多路召回→证据评分→补检索→计划→执行→校验→拒答）
- RAG 作为可信证据层
- Plan-then-Execute 执行策略

### 要做的事

#### 11.1 端到端集成

将阶段 8 的 hybrid_retriever 接入阶段 10 的 LangGraph 图中：
- `retrieve_node` 调用 `hybrid_retriever.search()`
- `grade_node` 对每个召回片段做 LLM-as-Judge 相关性评分
- `evidence_node` 判断评分后的相关片段是否足够
- `plan_node` 基于证据生成结构化计划
- `execute_node` 按计划执行工具（只读自动，写入型触发审批）
- `generate_node` 只用通过评分的相关片段 + 工具结果生成答案

#### 11.2 查询改写

```python
async def rewrite_query(query: str) -> list[str]:
    prompt = f"""
    请将以下企业知识库查询改写为 3 个不同角度的检索查询，
    以提高召回率。保留原始意图，但从不同表达方式提问。

    原始查询: {query}

    输出 JSON 数组，每个元素是一个改写后的查询字符串。
    """
    return await llm.generate_json(prompt)
```

#### 11.3 证据评分

```python
async def grade_chunks(query: str, chunks: list[dict]) -> list[dict]:
    """对每个召回片段做相关性评分"""
    prompt = f"""
    判断以下片段是否与问题相关。
    问题: {query}
    片段: {chunk.text}
    回答 "relevant" 或 "irrelevant"，并简要说明原因。
    """
    ...
```

#### 11.4 SSE 流式推送

```python
async def stream_agent_events(run_id: str):
    async for event in agent.stream(state):
        yield SSEEvent(
            event=event.type,  # step_start, retrieval_result, tool_call, approval_required, answer_token
            data=event.data,
        )
```

### 验证方式

1. 完整 Demo 流程："新员工入职到转正要办哪些事项？"
   - Agent 创建 run → 检索入职制度 → 评分 → 生成计划 → 生成办理清单 → 准备创建工单 → 暂停等审批 → 用户 approve → 恢复 → 返回完整答案 + citations + 工单结果
2. 证据不足时自动改写查询重试
3. 超出范围问题自动拒答
4. 每步都有 trace 记录
5. `pytest tests/test_e2e_agent.py` 通过

---

## 阶段 12：Grounded Answer 与评测

### 目标

实现证据约束的答案生成，构建 RAGAS 评测体系，确保答案可追溯、可量化。

### 对应能力

- Grounded Answer 可信回答（三条硬约束）
- RAGAS 评测闭环（Golden Dataset + A/B 对照）
- 评测指标：context_precision、context_recall、faithfulness、answer_relevancy

### 要做的事

#### 12.1 答案生成 Prompt

```
app/prompts/
├── __init__.py
├── templates.py            # Jinja2 prompt 模板
├── answer.py               # 答案生成 prompt（证据约束版）
├── factcheck.py            # 事实校验 prompt
└── refusal.py              # 拒答 prompt
```

#### 12.2 答案约束 Prompt

```python
ANSWER_PROMPT = """
你是一个企业知识库问答助手。请仅根据以下提供的证据回答用户问题。

## 证据
{% for evidence in evidence_bundle %}
[{{ loop.index }}] 来源: {{ evidence.document_name }}, {{ evidence.section }}, 第{{ evidence.page }}页
内容: {{ evidence.text }}
检索分数: {{ evidence.score }} | Rerank 分数: {{ evidence.rerank_score }}
{% endfor %}

{% if tool_results %}
## 工具执行结果
{% for result in tool_results %}
- {{ result.tool_name }}: {{ result.output }}
{% endfor %}
{% endif %}

## 用户问题
{{ question }}

## 硬约束
1. 仅使用证据中的信息回答，不编造。
2. 每个关键结论必须关联 [1][2][3] 引用编号。
3. 如果证据不足，回答"根据现有知识库，未找到相关依据"。
"""
```

#### 12.3 结构化引用返回

```json
{
  "answer": "新员工入职到转正通常包括...",
  "citations": [
    {
      "id": 1,
      "document_name": "员工入职与转正管理制度.md",
      "section": "试用期转正流程",
      "page": 3,
      "chunk_text": "转正前 7 天，直属主管需...",
      "score": 0.91,
      "rerank_score": 0.95
    }
  ],
  "confidence": 0.87,
  "tool_results": [
    {
      "tool_name": "create_mock_hr_ticket",
      "status": "approved",
      "output": "工单 HR-20260528-001 已创建"
    }
  ]
}
```

#### 12.4 RAGAS 评测框架

```
app/services/evaluation/
├── __init__.py
├── ragas_eval.py           # RAGAS 指标计算
├── eval_runner.py          # 评测执行器
├── ab_testing.py           # A/B 对照实验
└── golden_dataset.py       # Golden Dataset 管理
```

#### 12.5 Golden Dataset 格式

```jsonl
{"question": "新员工入职需要提交哪些材料？", "answer": "...", "contexts": ["..."], "ground_truth_docs": ["入职管理制度.md"], "ground_truth_sections": ["第二章 入职材料"], "expected_tools": [], "requires_approval": false}
```

#### 12.6 评测指标

```python
# RAG 指标
metrics = [
    "context_precision",    # 召回片段中与答案相关的比例
    "context_recall",       # 答案所需信息被召回覆盖的比例
    "faithfulness",         # 答案断言是否有证据支撑
    "answer_relevancy",     # 答案与问题的相关程度
]

# Agent 指标（自定义）
agent_metrics = [
    "tool_call_accuracy",       # 工具调用是否正确
    "approval_correctness",     # 审批决策是否合理
    "agent_goal_completion_rate",  # Agent 目标完成率
]
```

#### 12.7 API 端点

| 方法 | 路径 | 说明 |
|------|------|------|
| POST | `/api/v1/eval/run` | 执行评测（指定 dataset 和配置） |
| GET | `/api/v1/eval/runs` | 列出评测历史 |
| GET | `/api/v1/eval/runs/{id}` | 查看评测结果详情 |

#### 12.8 新增依赖

```toml
"ragas>=0.2",
"jinja2>=3.1",
"sse-starlette>=2.2",
```

### 验证方式

1. 答案中每个关键结论都有 [1][2] 引用
2. 引用的文档、页码、章节与答案内容匹配
3. 证据不足时返回拒答而非硬答
4. RAGAS 评测在 Golden Dataset 上运行：faithfulness > 0.6, context_precision > 0.6
5. A/B 对照：调整 chunk size 后能对比两个版本的指标差异
6. `pytest tests/test_evaluation.py` 通过

---

## 阶段 13：ACL 权限与安全防护

### 目标

实现文档级 ACL 三层权限控制和 OWASP LLM 风险防护。

### 对应能力

- 文档级 ACL 权限隔离（入库时、检索前、生成前三层控制）
- OWASP LLM / Agent 风险防护（Prompt Injection、敏感信息泄露、Excessive Agency 等）

### 要做的事

#### 13.1 ACL 模块

```
app/services/security/
├── __init__.py
├── acl.py                  # 权限过滤逻辑
├── input_validator.py      # Prompt Injection 检测
├── sensitive_redactor.py   # PII 脱敏
└── owasp.py                # OWASP 风险缓解
```

#### 13.2 三层权限控制

1. **入库时**：document 写入 tenant_id、department_id、visibility metadata
2. **检索前**：根据 user 构造 Milvus expr filter + ES query filter
   ```python
   # Milvus
   expr = f"tenant_id == '{user.tenant_id}' and (visibility == 'public' or department_id in {user.department_ids})"
   ```
3. **生成前**：校验 citations 中的每个 chunk 是否都在用户权限范围内

#### 13.3 OWASP 风险处理

| 风险 | 缓解措施 |
|------|----------|
| Prompt Injection | 输入模式检测 + 指令隔离 + 内容标注 |
| Sensitive Info Disclosure | PII 自动脱敏（身份证号、手机号、薪资） |
| System Prompt Leakage | 系统 prompt 包含"不泄露内部指令"约束 |
| Vector/Embedding Weaknesses | 文档入库安全扫描、embedding 隔离 |
| Excessive Agency | 工具白名单、权限 scope、审批流、速率限制 |
| Unbounded Consumption | 检索数量限制、上下文长度限制、重试次数限制 |

#### 13.4 Middleware

```
app/middleware/
├── __init__.py
├── auth.py                 # JWT 认证
├── rate_limit.py           # Redis 速率限制
└── tenant.py               # 多租户上下文注入
```

#### 13.5 新增依赖

```toml
"pyjwt>=2.10",
```

### 验证方式

1. 用户 A（HR 部门）上传机密文档 → 用户 B（工程部）查询 → 检索返回 0 条该文档结果
2. 发送 Prompt Injection 输入 → 安全层检测并拦截
3. 答案中包含身份证号 → 自动脱敏为 `110***********1234`
4. `pytest tests/test_acl.py` + `tests/test_security.py` 通过

---

## 阶段 14：可观测性与 Trace

### 目标

接入 Phoenix + OpenTelemetry，建立完整的链路追踪体系。

### 对应能力

- Phoenix + OpenTelemetry 可观测（trace 树：LLM call、retrieval、rerank、tool call、approval）
- 从"最终答案错了"定位到"具体哪一步出了问题"

### 要做的事

#### 14.1 Phoenix 集成

```python
# app/services/observability/phoenix_integration.py
from phoenix.otel import register

tracer_provider = register(
    project_name="enterprisemind-rag",
    endpoint="http://phoenix:6006",
)

# LangChain/LangGraph 自动埋点
from openinference.instrumentation.langchain import LangChainInstrumentor
LangChainInstrumentor().instrument(tracer_provider=tracer_provider)

# OpenAI-compatible 调用自动埋点
from openinference.instrumentation.openai import OpenAIInstrumentor
OpenAIInstrumentor().instrument(tracer_provider=tracer_provider)
```

#### 14.2 自定义 Span

```python
# app/services/observability/otel_tracer.py
from opentelemetry import trace

tracer = trace.get_tracer("enterprisemind")

@tracer.start_as_current_span("retrieval")
async def traced_retrieval(query: str, ...):
    span = trace.get_current_span()
    span.set_attribute("query", query)
    span.set_attribute("top_k", top_k)
    results = await hybrid_retriever.search(...)
    span.set_attribute("result_count", len(results))
    return results
```

#### 14.3 Trace 树结构

```text
AgentRun (root span)
├── IntentSpan (意图识别)
├── RewriteSpan (查询改写)
├── RetrievalSpan (检索)
│   ├── DenseSearchSpan
│   ├── BM25SearchSpan
│   └── RerankSpan
├── GradingSpan (证据评分)
├── PlanningSpan (计划生成)
├── ToolCallSpan (工具调用)
│   └── ApprovalSpan (审批)
├── AnswerSpan (答案生成)
├── FactcheckSpan (事实校验)
└── EvalSpan (评测)
```

#### 14.4 Docker Compose 添加 Phoenix

```yaml
phoenix:
  image: arizephoenix/phoenix:latest
  ports: ["6006:6006"]
  environment:
    PHOENIX_PORT: 6006
```

#### 14.5 新增依赖

```toml
"arize-phoenix>=5.0",
"openinference-instrumentation-langchain>=0.1",
"openinference-instrumentation-openai>=0.1",
"opentelemetry-api>=1.28",
"opentelemetry-sdk>=1.28",
```

### 验证方式

1. 执行一次完整问答后，Phoenix UI `http://localhost:6006` 显示完整 trace 树
2. 每个 span 有输入、输出、耗时、token 消耗
3. 能在 trace 中定位：检索命中了哪些片段、rerank 分数、LLM 输入的完整 prompt
4. 错误时 trace 中有错误信息

---

## 阶段 15：前端控制台

### 目标

构建用于运行观测与人工审批的前端控制台，呈现 Agent Harness + RAG 的完整执行链路。当前落地技术栈为 Vue 3 + Element Plus + Pinia + TypeScript。

### 对应能力

- 界面呈现 Agent Run 生命周期、审批面板、证据引用与 Trace 可视化

### 要做的事

#### 15.1 页面结构

```
frontend/
├── src/
│   ├── App.tsx
│   ├── pages/
│   │   ├── Documents.tsx         # 文档管理：上传、列表、状态
│   │   ├── Chat.tsx              # Agent 控制台：提问、执行、结果
│   │   ├── RunDetail.tsx         # Run 详情：步骤、证据、工具、审批
│   │   ├── Approvals.tsx         # 审批面板：待审批列表、决策
│   │   ├── Trace.tsx             # Trace 可视化：span 树、耗时、token
│   │   └── Evaluation.tsx        # 评测结果：指标、A/B 对比
│   ├── components/
│   │   ├── DocumentUpload.tsx
│   │   ├── ChatMessage.tsx
│   │   ├── CitationPanel.tsx     # 证据引用面板（独立渲染）
│   │   ├── ApprovalCard.tsx      # 审批卡片（工具名、参数、影响、选项）
│   │   ├── StepTimeline.tsx      # 步骤时间线
│   │   ├── TraceTree.tsx         # Trace 树可视化
│   │   └── EvalChart.tsx         # 评测指标图表
│   └── hooks/
│       ├── useAgentRun.ts        # SSE 连接、步骤流式更新
│       └── useDocuments.ts
├── package.json
└── Dockerfile
```

#### 15.2 核心交互流程

1. **文档管理页**：上传文档 → 查看入库状态（PENDING → PARSING → ... → READY）
2. **Agent 控制台**：
   - 输入问题
   - 实时显示 Agent 执行步骤（SSE 流式推送）
   - 显示检索到的证据片段和分数
   - 显示执行计划
   - 如触发审批 → 弹出审批卡片（工具名、参数、预期影响、Approve/Edit/Reject）
   - 审批后继续执行
   - 最终显示答案 + citations + 工具结果
3. **Run 详情页**：查看完整执行历史、每个 step 的输入输出
4. **Trace 页**：span 树可视化、耗时分布、token 消耗
5. **评测页**：指标图表、A/B 对比

#### 15.3 Docker Compose

```yaml
frontend:
  build: ./frontend
  ports: ["3000:3000"]
  depends_on: [api]
```

### 验证方式

1. 通过 UI 上传文档 → 状态实时更新
2. 提问后实时看到 Agent 执行步骤
3. 触发审批时弹出审批卡片，Approve 后继续执行
4. 最终答案有独立的引用面板
5. Trace 页能展示完整 span 树

---

## 阶段 16：V2 增强 — GraphRAG / LightRAG 与 MCP

### 目标

扩展 GraphRAG 关系增强和 MCP 标准化接口。

### 对应能力

- GraphRAG / LightRAG 关系增强（V2 增强方向）
- MCP 标准化接口预留

### 要做的事

#### 16.1 LightRAG 集成

```
app/services/graph/
├── __init__.py
├── lightrag_service.py     # LightRAG 初始化和管理
├── entity_extractor.py     # 实体关系抽取
└── graph_retriever.py      # 图检索（multi_hop 意图触发）
```

#### 16.2 图检索与 Agent 集成

- intent_node 新增 "multi_hop" 意图分类
- retrieve_node：当意图为 multi_hop 时，同时调用 hybrid_retriever 和 graph_retriever
- graph_retriever：从图谱定位实体关系链，补充原文证据

#### 16.3 MCP 接口（原规划为预留，阶段 17 已实现本地 reference）

```
app/services/mcp/
├── __init__.py
├── server.py               # MCP Server 定义（V2）
├── resources.py            # resources: 制度文档、章节、引用片段
├── tools.py                # tools: 检索、生成清单、创建模拟工单
└── prompts.py              # prompts: 问答、流程规划模板
```

MCP 设计要点：

- 以 JSON-RPC 消息模型组织交互。
- 按 client-host-server 架构隔离不同工具服务。
- 通过 capability negotiation 暴露 `tools`、`resources`、`prompts`。
- 对远程 HTTP transport 预留 authorization 和 scope consent。
- tool response 使用 structured tool output，便于 Agent Harness 记录审计和生成引用。

#### 16.4 新增依赖

```toml
"lightrag-hku>=1.3",
"networkx>=3.4",
```

### 验证方式

1. 入库 10+ 文档 → 实体抽取正确（部门、岗位、制度、流程）
2. 多跳问题"某岗位入职到转正涉及哪些制度和审批人" → 图谱检索返回关联结果
3. MCP Server 能通过 MCP Inspector 或 Claude Desktop 连接并调用工具

---

## 阶段依赖关系图

```
阶段 1（基础设施）
  │
阶段 2（数据模型）
  │
阶段 3（文档上传）
  │
阶段 4（异步入库）──────────────────────┐
  │                                      │
阶段 5（Docling 解析，升级阶段 4）        │
  │                                      │
阶段 6（多粒度 Chunking）                │
  │                                      │
阶段 7（Embedding + 索引）               │
  │                                      │
阶段 8（混合检索 + Reranker）            │
  │                                      │
阶段 9（Agent Harness 核心）             │
  │                                      │
阶段 10（LangGraph + HITL）             │
  │                                      │
阶段 11（Agentic RAG 完整链路）          │
  │                                      │
阶段 12（Grounded Answer + 评测）        │
  │                                      │
阶段 13（ACL + 安全）                    │
  │                                      │
阶段 14（可观测性）                      │
  │                                      │
阶段 15（前端控制台）                   │
  │                                      │
阶段 16（V2：GraphRAG + MCP）←──────────┘
```

说明：
- 阶段 5 升级阶段 4 的 Parser，可部分并行
- 阶段 13（ACL）可以提前到阶段 8 之后开始
- 阶段 14（可观测性）可以从阶段 11 开始逐步接入
- 阶段 16（V2）可与阶段 15 并行

---

## 完整依赖清单

```toml
[project]
dependencies = [
    # === 核心框架 ===
    "fastapi>=0.115",
    "uvicorn[standard]>=0.34",
    "pydantic-settings>=2.7",
    "python-dotenv>=1.0",
    "python-multipart>=0.0.18",
    "httpx>=0.28",
    "sse-starlette>=2.2",
    "jinja2>=3.1",

    # === 数据库 ===
    "sqlalchemy[asyncio]>=2.0",
    "asyncpg>=0.30",
    "alembic>=1.14",

    # === 缓存 / 队列 ===
    "redis[hiredis]>=5.2",
    "celery[redis]>=5.4",

    # === 向量 / 搜索 ===
    "pymilvus[model]>=2.5",
    "elasticsearch[async]>=8.13",

    # === 对象存储 ===
    "minio>=7.2",

    # === 文档解析 ===
    "docling>=2.0",
    "pymupdf>=1.25",
    "python-docx>=1.1",
    "python-pptx>=1.0",
    "openpyxl>=3.1",

    # === Embedding / Reranker ===
    "sentence-transformers>=3.4",
    "FlagEmbedding>=1.3",
    "tiktoken>=0.8",

    # === LLM ===
    "openai>=1.60",

    # === Agent ===
    "langgraph>=0.3",
    "langchain-core>=0.3",
    "langchain-openai>=0.3",

    # === 知识图谱（V2）===
    "lightrag-hku>=1.3",
    "networkx>=3.4",

    # === 评测 ===
    "ragas>=0.2",

    # === 可观测性 ===
    "arize-phoenix>=5.0",
    "openinference-instrumentation-langchain>=0.1",
    "openinference-instrumentation-openai>=0.1",
    "opentelemetry-api>=1.28",
    "opentelemetry-sdk>=1.28",

    # === 安全 ===
    "pyjwt>=2.10",
]
```

---

## 最终项目目录结构

```
enterprisemind-rag/
├── docker-compose.yml
├── docker-compose.app.yml
├── .env / .env.example / .gitignore
├── pyproject.toml
├── alembic.ini
├── alembic/
├── scripts/
├── frontend/                           # Vue 3 前端控制台
│   ├── src/
│   ├── package.json
│   └── Dockerfile
├── app/
│   ├── __init__.py
│   ├── main.py
│   ├── config.py
│   ├── dependencies.py
│   ├── core/                           # 基础设施连接
│   ├── models/                         # SQLAlchemy Models
│   ├── schemas/                        # Pydantic Schemas
│   ├── middleware/                      # 认证、限流、租户
│   ├── api/
│   │   └── v1/
│   │       ├── router.py
│   │       ├── health.py
│   │       ├── documents.py
│   │       ├── tasks.py
│   │       ├── chat.py
│   │       ├── agent.py
│   │       ├── admin.py
│   │       └── eval.py
│   ├── services/
│   │   ├── document_service.py
│   │   ├── parser/                     # 文档解析
│   │   ├── chunker/                    # 多粒度分块
│   │   ├── embedding/                  # Embedding 服务
│   │   ├── indexer/                    # Milvus + ES 索引
│   │   ├── retrieval/                  # 混合检索 + Reranker
│   │   ├── llm/                        # LLM 调用封装
│   │   ├── agent/                      # Agent Harness 核心
│   │   │   ├── graph.py
│   │   │   ├── state.py
│   │   │   ├── run_manager.py
│   │   │   ├── tool_registry.py
│   │   │   ├── tool_executor.py
│   │   │   ├── plan_manager.py
│   │   │   ├── prompts.py
│   │   │   └── nodes/
│   │   ├── tools/                      # V1 模拟工具
│   │   ├── graph/                      # LightRAG（V2）
│   │   ├── evaluation/                 # RAGAS 评测
│   │   ├── security/                   # ACL + OWASP
│   │   └── observability/              # Phoenix + OTel
│   ├── prompts/                        # Prompt 模板
│   └── worker/                         # Celery Worker
│       ├── celery_app.py
│       └── tasks/
└── tests/
    ├── conftest.py
    ├── test_health.py
    ├── test_documents.py
    ├── test_ingestion.py
    ├── test_parsers.py
    ├── test_chunkers.py
    ├── test_contextual.py
    ├── test_indexing.py
    ├── test_hybrid_retrieval.py
    ├── test_agent_run.py
    ├── test_tool_registry.py
    ├── test_agent_graph.py
    ├── test_e2e_agent.py
    ├── test_evaluation.py
    ├── test_acl.py
    ├── test_security.py
    ├── fixtures/
    └── eval/
        └── golden_dataset.jsonl
```
