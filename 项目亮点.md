# EnterpriseMind Agent Harness RAG：企业知识任务智能体平台

> 本文档综合 `RAG项目面试亮点.md` 与原 `项目亮点.md`，用于统一项目亮点、技术叙事和面试表达。
> 项目核心不是普通 RAG Chatbot，而是：**RAG 作为可信证据层，Agent Harness 作为智能体执行治理层**。

---

## 1. 项目定位与简历版本

**项目名称：** EnterpriseMind Agent Harness RAG：企业知识任务智能体平台

**项目描述：**  
面向企业内部制度、合同、操作手册、会议纪要、产品文档等知识资产分散、检索低效、回答不可追溯、智能体执行不可控的问题，设计并实现一套 **自研 Agent Harness + RAG** 平台。系统将企业知识库 RAG 作为证据层，将 Agent Harness 作为执行治理层，支持多格式文档入库、结构化解析、混合检索、证据引用、Agent Run 生命周期管理、工具审批、ACL 权限过滤、审计追踪、低置信度拒答和评测闭环。  
在 HR 制度流程场景下，用户不仅可以获得有引用来源的可信回答，还可以让 Agent 基于制度证据生成办理计划，并在人工审批后调用模拟 HR 工具创建工单。

**技术栈：**  
Python、FastAPI、LangGraph、LangChain Core、Celery、Redis、PostgreSQL、Milvus、Elasticsearch / BM25、Docling / MinerU、Qwen Cloud `text-embedding-v4`、`qwen3-rerank`、Qwen3-Embedding / Qwen3-Reranker、BGE-Reranker、RAGAS、Phoenix、OpenTelemetry、OpenInference、SSE / WebSocket、MCP、Vue 3、Element Plus、TypeScript、Docker Compose。

**一句话亮点：**  
这个项目不是“检索后生成答案”的普通 RAG，而是把 RAG 检索证据、Agent 任务规划、工具审批、状态恢复、权限控制、trace 和评测放进同一个可治理的 Agent Harness 里。

---

## 2. 核心亮点总览

### 2.1 自研 Agent Harness 治理层

将一次用户请求建模为完整的 **Agent Run**，记录从意图识别、计划生成、检索证据、工具调用、人工审批、恢复执行到最终回答的全过程。Harness 统一管理状态机、工具权限、审批记录、审计日志和失败兜底，让 Agent 的每一步都可解释、可暂停、可恢复、可追踪。

### 2.2 LangGraph 有状态编排

使用 LangGraph 作为长运行、有状态、可中断、可恢复的 agent orchestration runtime。它适合承载 durable execution、streaming、human-in-the-loop、persistence 等能力：当 Agent 触发工具审批或需要用户补充信息时，可以中断执行、保存状态，并在用户确认后从同一 thread / checkpoint 恢复。

### 2.3 Human-in-the-Loop 工具审批

系统区分只读工具和写入型工具。只读工具如 `policy_search`、`get_user_profile` 可以自动执行；写入型工具如 `create_mock_hr_ticket` 必须先生成工具调用预览，等待用户 Approve / Edit / Reject。审批记录会写入 Agent Run，形成完整审计轨迹，避免 Agent 在企业场景中无约束执行副作用操作。

### 2.4 RAG 作为可信证据层

RAG 不只是给模型塞上下文，而是为 Agent 的计划、回答和工具动作提供证据。每个 evidence 都包含来源文档、页码或章节、chunk 摘要、检索分数、rerank 分数和 ACL 元数据。模型只能基于可见证据回答；证据不足、冲突或低置信时，系统拒答或追问。

### 2.5 混合检索与二阶段重排

检索链路结合 Dense 语义检索、Sparse / BM25 精确检索、RRF 融合和 reranker 精排。云端优先使用 Qwen Cloud `text-embedding-v4` 与 `qwen3-rerank`；本地或私有化场景可替换为开源 Qwen3-Embedding / Qwen3-Reranker 或 BGE 系列模型。这样既能处理“转正流程怎么走”这类语义问题，也能命中“HR-2026-04 第 12 条”这类编号、条款和专有名词查询。

### 2.6 多格式复杂文档入库

基于异步任务流处理 PDF、DOCX、XLSX、PPTX、Markdown、HTML、CSV、图片等企业资料。Docling 可将多格式输入转换为统一文档表示，并导出 Markdown、JSON、Text 等结果；MinerU 可作为复杂 PDF、扫描件和版面解析增强选项。解析结果保留标题层级、页码、表格、图片说明、阅读顺序和章节结构。

### 2.7 多粒度 Chunking 与 Contextual Retrieval

采用“结构化分块 + 语义分块 + Parent-Child Chunk + Contextual Retrieval”组合策略。先按标题、章节、表格、列表生成父节点，再在语义完整边界内生成子 chunk；检索时用子 chunk 精确命中问题，用父 chunk 补充上下文，并为每个 chunk 添加 50-100 token 左右的上下文前缀，缓解片段脱离原文后的召回偏差。

### 2.8 企业级 ACL 与安全边界

文档入库时记录租户、部门、角色、密级、可见范围等 metadata。检索前根据用户身份构造过滤条件，只召回有权限文档；生成答案前再次校验引用片段，避免无权限内容进入上下文或最终答案。安全设计参考 OWASP LLM / GenAI 风险，重点处理 Prompt Injection、Sensitive Information Disclosure、System Prompt Leakage、Vector and Embedding Weaknesses、Excessive Agency、Unbounded Consumption 等问题。

### 2.9 评测与可观测闭环

使用 RAGAS 评估 Context Precision、Context Recall、Faithfulness、Answer Relevancy / Answer Accuracy 等 RAG 指标，并扩展 Agent 侧指标，如 tool call accuracy、approval correctness、goal completion rate。Phoenix 基于 OpenTelemetry / OpenInference 记录模型调用、检索、rerank、工具调用、自定义逻辑和评测结果，用 trace 反向定位召回、prompt、工具或权限问题。

### 2.10 MCP 接口预留

将企业知识库能力抽象为 MCP tool / resource / prompt，后续可作为标准化上下文服务接入 ChatGPT、Claude、内部 Agent 或办公系统。MCP 采用 JSON-RPC、client-host-server 架构和 capability negotiation；较新规范还引入 authorization、structured tool output、tool metadata 和更细粒度的 scope consent。项目中 MCP 作为 V2 扩展，不在 V1 过早实现。

---

## 3. Agent Harness 治理层亮点

### 3.1 Agent Run 生命周期管理

每次用户请求都会创建一个 `AgentRun`，包括：

```text
CREATED
  -> RUNNING
  -> RETRIEVING_EVIDENCE
  -> PLANNING
  -> AWAITING_APPROVAL
  -> RESUMED
  -> COMPLETED / FAILED / CANCELLED
```

每个 step 记录：

- 输入与输出
- 节点名称
- 执行状态
- 耗时
- token 消耗
- 检索证据
- 工具调用
- 审批结果
- 错误信息

这样面试时可以强调：系统不是黑盒 Agent，而是把智能体执行过程做成可审计、可回放的工作流。

### 3.2 Plan-then-Execute 执行策略

Agent 不直接执行工具，而是先生成结构化计划：

```json
{
  "goal": "生成新员工入职到转正办理清单，并准备创建 HR 工单",
  "steps": [
    {"name": "检索入职制度", "tool": "policy_search"},
    {"name": "生成办理清单", "tool": "generate_hr_checklist"},
    {"name": "创建模拟 HR 工单", "tool": "create_mock_hr_ticket", "requires_approval": true}
  ]
}
```

计划可以被用户查看、审批或修改。执行过程中如果发现证据不足、工具失败或用户拒绝审批，Agent 会进入 re-plan 或 graceful stop，而不是盲目重试。

### 3.3 Human-in-the-Loop 审批与恢复

写入型工具触发前，Harness 生成审批请求：

- 工具名称
- 工具参数
- 预期影响
- 引用证据
- 风险级别
- 审批选项：Approve / Edit / Reject

用户批准后，系统用相同 run id / thread id 从 checkpoint 恢复，不重复执行已完成的检索和计划步骤。这个设计可以体现企业场景里对敏感动作的控制能力，尤其适合 HR 工单、审批流程、邮件发送、数据修改等场景。

### 3.4 Tool Registry 与权限分级

所有工具通过 Tool Registry 注册：

```json
{
  "name": "create_mock_hr_ticket",
  "description": "创建模拟 HR 工单",
  "permission_scope": "hr.ticket.write",
  "risk_level": "write",
  "requires_approval": true,
  "timeout_seconds": 10,
  "idempotent": true
}
```

V1 工具：

- `policy_search`：检索制度证据，只读。
- `get_user_profile`：读取当前用户部门和角色，只读。
- `generate_hr_checklist`：基于证据生成办理清单，只读。
- `ask_clarification`：证据不足时追问用户，只读。
- `create_mock_hr_ticket`：创建模拟 HR 工单，写入型，必须审批。

### 3.5 审计日志与 Trace

Agent Harness 对每个 run 生成 trace 树：

```text
AgentRun
  -> IntentSpan
  -> RetrievalSpan
  -> RerankSpan
  -> PlanningSpan
  -> ToolCallSpan
  -> ApprovalSpan
  -> AnswerSpan
  -> EvalSpan
```

这些 trace 可接入 Phoenix / OpenTelemetry，用于定位：

- 哪一步耗时最高
- 哪个查询召回失败
- 哪个工具经常被拒绝
- 哪个 prompt 版本导致答案忠实度下降
- 哪类用户问题最容易触发低置信兜底

---

## 4. RAG 证据层亮点

### 4.1 异步文档入库流水线

基于 FastAPI + Celery + Redis 构建异步入库：

```text
上传文档
  -> 创建 ingestion_task
  -> 后台解析
  -> 清洗与结构识别
  -> 元数据与 ACL 标注
  -> Chunking
  -> Contextual Prefix 生成
  -> Embedding / Sparse 表示生成
  -> 写入 Milvus / Elasticsearch / PostgreSQL
  -> 返回入库状态与引用映射
```

优点：

- 上传接口快速返回任务 ID。
- 大文件解析不阻塞请求链路。
- 每个阶段可记录进度、失败原因和重试次数。
- 失败后可从单阶段重试，不必整个流程重来。

### 4.2 复杂文档结构化解析

Docling 作为主解析 adapter，处理 PDF、DOCX、XLSX、PPTX、Markdown、HTML、CSV、图片、音视频等输入，并统一导出 Markdown、JSON、Text 等格式。MinerU 作为复杂 PDF、扫描件、表格密集文档和版面分析增强选项。

解析时保留：

- 标题层级
- 页码
- 表格结构
- 图片说明
- 章节关系
- 阅读顺序
- 原始文件 metadata
- ACL metadata

这样后续 citation 不只是“来自某文档”，而能明确到页码、章节、表格或段落。

### 4.3 多粒度 Chunking

采用四层分块策略：

1. **结构化分块**：按标题、章节、表格、列表切出 parent chunk。
2. **语义分块**：在 parent chunk 内按语义完整性切 child chunk。
3. **Parent-Child 检索**：检索命中 child chunk，生成答案时补 parent context。
4. **表格保护**：表格内容按行组、表头和上下文一起保存，避免列含义丢失。

相比固定长度切分，这种方案能减少语义断裂、表格错位和章节上下文丢失。

### 4.4 Contextual Retrieval

为每个 chunk 生成上下文前缀，例如：

```text
该片段来自《员工入职与转正管理制度》第 3 章“试用期转正流程”，
讨论新员工在转正前需要由直属主管发起评估，并由 HR 复核归档。
```

前缀与 chunk 正文一起参与 embedding 和 BM25 索引。这样用户问“谁发起转正评估”时，即使原文片段很短，也能因为章节、对象和业务语义被更稳定地召回。

### 4.5 Dense + Sparse 混合检索

检索链路：

```text
用户问题
  -> 查询改写
  -> ACL 过滤条件生成
  -> Dense Vector Search
  -> Sparse / BM25 Search
  -> RRF 融合
  -> Reranker 精排
  -> Evidence Bundle
```

技术取舍：

- Milvus multi-vector hybrid search 用于 dense / sparse、多字段、多模态检索。
- Elasticsearch / BM25 用于制度编号、条款、专有名词和精确关键词。
- RRF 用于融合不同召回源的排序结果。
- Reranker 用于对 top-N 候选做 query-chunk 级别精排。

### 4.6 Embedding 与 Reranker 策略

云端优先：

- `text-embedding-v4`：用于文本检索、RAG、聚类等场景，维度可根据成本和精度选择。
- `qwen3-rerank`：用于重排召回候选，提升 top-k 相关性。

本地或私有化可选：

- Qwen3-Embedding / Qwen3-Reranker 开源模型。
- BGE-M3 / BGE-Reranker 系列。
- FlagEmbedding 作为本地 rerank 实现方向。

文档中不再把某个固定维度或某个单一模型写死，避免技术表述过时。

### 4.7 Grounded Answer 可信回答

答案生成遵循三条硬约束：

1. 只能基于 evidence 中的片段回答。
2. 每个关键结论必须能关联到 citation。
3. 证据不足、冲突或低置信时，必须拒答或追问。

返回结构：

```json
{
  "answer": "新员工入职到转正通常包括资料提交、账号开通、导师确认、试用期目标确认、转正评估和 HR 归档。",
  "citations": [
    {
      "source": "员工入职与转正管理制度.md",
      "section": "试用期转正流程",
      "page": 3,
      "score": 0.91
    }
  ],
  "confidence": 0.87
}
```

这能把“可信回答”从口号变成接口和数据结构。

### 4.8 GraphRAG / LightRAG 关系增强

GraphRAG / LightRAG 作为 V2 增强方向，不强行塞入 V1 主链路。适合处理跨文档、多实体、多跳关系问题，例如：

> 某岗位从入职到转正涉及哪些制度、系统和审批人？

处理思路：

1. 从文档中抽取部门、岗位、制度、流程、系统、负责人、审批节点。
2. 构建轻量实体关系图。
3. 当意图分类为 multi-hop 时，先走图谱定位关系链。
4. 再用向量 / BM25 检索补充原文证据。

面试时可以强调：图谱不是替代 RAG，而是增强跨文档关系推理。

---

## 5. 企业级安全、权限、评测与可观测

### 5.1 文档级 ACL 权限隔离

权限不能只在前端做，也不能只在答案生成后过滤。系统在三个位置做权限控制：

1. **入库时**：写入租户、部门、角色、密级、可见范围等 metadata。
2. **检索前**：根据用户身份生成 Milvus filter 和 Elasticsearch filter。
3. **生成前**：二次校验 citations 是否都在用户权限范围内。

这样可以防止向量库召回无权限内容，也避免敏感内容进入 LLM 上下文。

### 5.2 OWASP LLM / Agent 风险防护

重点覆盖：

- **Prompt Injection**：用户输入、文档内容和工具返回都可能携带恶意指令，需要做指令隔离、内容标注和 prompt 防护。
- **Sensitive Information Disclosure**：对身份证号、手机号、薪资、合同金额等敏感信息做脱敏、权限校验和日志保护。
- **System Prompt Leakage**：禁止泄露系统 prompt、工具 schema、内部策略和基础设施信息。
- **Vector and Embedding Weaknesses**：防止 RAG 污染、恶意文档注入、embedding 泄露和越权召回。
- **Excessive Agency**：通过工具白名单、权限 scope、审批流和速率限制约束 Agent 行为。
- **Unbounded Consumption**：限制单次检索数量、上下文长度、工具调用次数和重试次数。

### 5.3 RAGAS 评测闭环

构建 Golden Dataset，例如 50+ HR 标准问答对，包含：

- 用户问题
- 标准答案
- 期望引用文档
- 期望命中章节
- 允许调用的工具
- 是否需要审批

核心指标：

- `context_precision`
- `context_recall`
- `faithfulness`
- `answer_relevancy`
- `answer_accuracy`
- `tool_call_accuracy`
- `approval_correctness`
- `agent_goal_completion_rate`

每次调整 chunking、embedding、reranker、prompt、工具策略后，用同一批样例做 A/B 对照，避免“凭感觉调参”。

### 5.4 Phoenix + OpenTelemetry 可观测

Phoenix 作为 AI observability 与 evaluation 平台，用 OpenTelemetry / OpenInference 接收 trace。它能记录：

- LLM call
- Embedding call
- Retrieval span
- Reranker span
- Tool call
- Approval span
- Guardrail span
- Evaluator span

这使得排查问题可以从“最终答案错了”变成“具体是哪一步错了”：解析漏了表格、chunk 切断语义、BM25 没命中编号、reranker 排错、prompt 越界回答，还是工具参数生成错误。

### 5.5 MCP 标准化接口

V2 可将企业知识库封装为 MCP Server，暴露：

- `resources`：制度文档、章节、引用片段。
- `tools`：检索知识库、生成清单、创建模拟工单。
- `prompts`：企业问答、流程规划、审批说明模板。

MCP 当前强调 JSON-RPC、client-host-server 架构、capability negotiation、authorization、structured tool output 和更细粒度的 scope consent。项目中预留 MCP adapter，可以让企业知识库未来被不同 Agent 或办公系统复用。

---

## 6. 可落地架构链路

### 6.1 文档入库链路

```text
用户上传 HR 制度文档
  -> FastAPI 接收文件并创建 ingestion_task
  -> Celery Worker 后台处理
  -> Docling / MinerU 解析为 Markdown / JSON / Text
  -> 清洗、结构识别、表格保护、页码映射
  -> 写入文档 metadata 与 ACL
  -> 结构化分块 + 语义分块 + Parent-Child Chunk
  -> Contextual Prefix 生成
  -> Embedding / Sparse 表示生成
  -> 写入 Milvus、Elasticsearch、PostgreSQL
  -> 返回入库状态、引用映射和错误信息
```

### 6.2 Agent Harness 执行链路

```text
用户提出任务
  -> 创建 AgentRun
  -> 意图识别
  -> 查询改写
  -> ACL-aware RAG 检索
  -> Rerank 与证据评分
  -> 判断证据充分性
  -> 生成结构化 Plan
  -> 判断是否需要工具
  -> 只读工具自动执行
  -> 写入型工具触发审批 interrupt
  -> 用户 Approve / Edit / Reject
  -> checkpoint resume
  -> 生成 Grounded Answer
  -> 返回 citations、tool result、trace、eval metadata
```

### 6.3 存储与索引

```text
PostgreSQL
  -> documents
  -> chunks
  -> agent_runs
  -> agent_steps
  -> tool_calls
  -> approvals
  -> eval_cases
  -> eval_runs

Milvus
  -> dense vectors
  -> sparse vectors / multi-vector fields
  -> metadata filters

Elasticsearch
  -> BM25 index
  -> keyword / policy code / section search

Object Storage / Local FS
  -> original files
  -> parsed markdown/json
```

---

## 7. 面试可讲版本

### 7.1 这个项目和普通 RAG 有什么区别？

普通 RAG 通常是“问题 -> 检索 -> 生成答案”，流程比较短，检索失败时可能仍然硬答。我的项目把 RAG 作为 Agent 的证据层，把 Agent Harness 作为执行治理层。除了检索和回答，它还管理 Agent Run 生命周期、工具注册、人工审批、状态恢复、权限过滤、审计日志和评测指标。也就是说，它不只是回答问题，而是把企业知识任务变成可控、可追踪、可恢复的 agent 工作流。

### 7.2 为什么需要 Agent Harness？

因为企业场景里的 Agent 不能自由发挥。它可能会创建工单、发起审批、发送邮件或修改状态，这些都是有副作用的动作。Agent Harness 的作用就是给 Agent 加上运行边界：哪些工具能用、哪些动作要审批、执行到哪一步、证据来自哪里、失败后怎么恢复、日志怎么审计。这样系统才有生产可控性。

### 7.3 为什么写入型工具必须审批？

只读检索错了最多是答案不准，但写入型工具错了可能会造成真实业务影响。比如创建错误 HR 工单、发错邮件、误触审批流程。所以系统在写入型工具前会暂停，展示工具名、参数、预期影响和引用证据，等用户确认后再执行。这是 Human-in-the-Loop 在企业 Agent 里的核心价值。

### 7.4 为什么不用单纯向量检索？

企业文档里有大量制度编号、条款编号、部门名称、系统字段和专有名词。纯向量检索擅长语义相似，但不一定能精确命中“HR-2026-04 第 12 条”。所以我使用 Dense + Sparse 混合检索：向量负责语义泛化，BM25 / sparse 负责关键词和编号精确匹配，再用 RRF 融合和 reranker 精排。

### 7.5 Contextual Retrieval 解决什么问题？

传统 chunk 切完后会丢失原文上下文，比如“转正前 7 天发起评估”单独看不知道属于哪份制度、哪个岗位、哪个流程。Contextual Retrieval 会给 chunk 添加短上下文前缀，说明文档、章节、对象和业务含义，再一起进入 embedding 和 BM25 索引。这样检索命中更稳定，引用也更准确。

### 7.6 如何判断答案是不是幻觉？

我从三层判断：第一，答案的关键断言是否能在 evidence 中找到支撑；第二，citation 的文档、章节、页码是否匹配断言；第三，用 RAGAS 的 faithfulness / answer relevancy 等指标做离线评测。如果证据不足或冲突，Agent 不应该硬答，而是拒答或追问。

### 7.7 权限隔离怎么做？

权限必须在检索前就生效，而不是答案出来后再过滤。文档入库时写入租户、部门、角色、密级等 metadata；检索前根据用户身份构造 filter，只召回有权限的 chunk；生成答案前再校验 citations，确保没有越权内容进入上下文或输出。

### 7.8 如果检索结果不准，怎么排查？

按链路排查：先看解析有没有丢标题、页码、表格；再看 chunk 是否切断语义；然后看 query rewrite 是否改坏问题；接着看 dense、BM25、RRF、reranker 各自 top-k；最后看 prompt 是否严格要求基于 evidence 回答。配合 Phoenix trace 可以看到每一步输入输出、耗时和分数。

### 7.9 为什么要做 RAGAS 和 Phoenix？

RAG 系统不能靠肉眼感觉调参。RAGAS 用固定评测集衡量召回质量和答案忠实度；Phoenix 记录每次运行的 trace、span、模型调用、检索结果和工具调用。两者结合可以做到：离线有指标，线上有链路，问题能复现，优化能对比。

### 7.10 GraphRAG 是否一定要做？

不一定。GraphRAG 更适合跨文档、多实体、多跳关系问题，比如“某岗位入职到转正涉及哪些制度和审批人”。V1 可以先用 Agent Harness + 混合检索跑通主链路，GraphRAG 作为 V2 增强。这样技术路线更稳，不会为了堆亮点牺牲可落地性。

---

## 8. 简历压缩版

**EnterpriseMind Agent Harness RAG 企业知识任务智能体平台**

面向企业内部制度、合同、手册、会议纪要等多格式知识资料，构建自研 Agent Harness + RAG 平台。系统以 RAG 作为可信证据层，基于 Docling / MinerU 完成复杂文档解析，通过多粒度 chunking、Contextual Retrieval、Milvus multi-vector hybrid search、Elasticsearch BM25、RRF 融合和 Qwen Cloud `text-embedding-v4` / `qwen3-rerank` 提升中文企业知识召回稳定性；以 Agent Harness 作为执行治理层，基于 LangGraph 编排长运行、有状态、可中断、可恢复的 Agent Run，支持 Plan-then-Execute、Human-in-the-Loop 工具审批、Tool Registry、checkpoint/resume、审计日志和低置信度拒答；结合文档级 ACL、OWASP LLM 风险防护、RAGAS 评测和 Phoenix / OpenTelemetry trace，实现企业知识任务的可信回答、可控执行和可观测优化。

---

## 9. 最新参考资料

- [LangGraph overview](https://docs.langchain.com/oss/python/langgraph/overview)：LangGraph 是面向长运行、有状态 agent 的 orchestration runtime，强调 durable execution、streaming、human-in-the-loop 和 persistence。
- [LangGraph interrupts / human-in-the-loop](https://docs.langchain.com/oss/python/langgraph/human-in-the-loop)：说明 `interrupt()`、checkpoint、thread id 和 resume 机制，适合工具审批与人工确认。
- [Qwen Cloud embedding & reranking](https://docs.qwencloud.com/developer-guides/getting-started/embedding-models)：文本检索和 RAG 推荐 `text-embedding-v4`，重排使用 `qwen3-rerank`。
- [Qwen3 Embedding Technical Report](https://arxiv.org/abs/2506.05176)：Qwen3-Embedding / Qwen3-Reranker 开源模型系列，可作为私有化部署选项。
- [Milvus Multi-Vector Hybrid Search](https://milvus.io/docs/multi-vector-search.md)：支持多向量字段、dense / sparse 和多模态 hybrid search。
- [Docling supported formats](https://docling-project.github.io/docling/usage/supported_formats/)：支持 PDF、DOCX、XLSX、PPTX、Markdown、HTML、CSV、图片、音频和视频等格式，并导出 Markdown、JSON、Text 等结果。
- [RAGAS Metrics](https://docs.ragas.io/en/latest/concepts/metrics/)：提供 RAG 与 agentic workflow 相关评测指标。
- [Phoenix docs](https://arize.com/docs/phoenix)：Phoenix 基于 OpenTelemetry / OpenInference 支持 tracing、evaluation、datasets 和 experiments。
- [OWASP Top 10 for LLM Applications](https://owasp.org/www-project-top-10-for-large-language-model-applications/)：LLM / GenAI 应用安全风险参考。
- [MCP versioning](https://modelcontextprotocol.io/docs/learn/versioning)：MCP 使用 `YYYY-MM-DD` 形式的协议版本标识。
- [MCP 2025-11-25 changelog](https://modelcontextprotocol.io/specification/2025-11-25/changelog)：包含 authorization、tool metadata、incremental scope consent 等新变化。
