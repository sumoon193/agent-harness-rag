# EnterpriseMind：企业制度型长流程 Agent Runtime

## 项目定位

EnterpriseMind 是面向企业内部制度型长流程的 Agent Runtime 与执行治理平台。HR Shared Service 是首个 Reference Application，用“新员工入职到转正”的长期 Case 验证证据检索、跨天执行、人工审批、权限控制、故障恢复、幂等副作用和审计。

一句话版本：

> RAG 负责回答“依据是什么”，Agent Harness 负责决定“是否允许执行、如何恢复、如何证明没有重复或越权执行”。

## 解决的问题

企业制度有版本、适用范围和权限差异；办理过程跨员工、主管、HR 和 IT，持续数天或数月；工单、任务和状态更新会产生副作用；审批在第二天发生时，人员权限和制度证据可能已经变化。

普通 RAG 缺少执行治理，固定 BPM 又无法理解非结构化制度和处理例外。项目的价值是把制度理解与确定性执行治理组合起来。

## 简历描述

设计并实现企业制度型长流程 Agent Runtime，以 RAG 作为版本化可信证据层，以 Agent Harness 管理 Case/Run 生命周期、计划、工具风险、人工审批、checkpoint/resume、权限、审计和 safety eval。实现 append-only Event Store、transactional outbox、optimistic concurrency、fencing lease、effectively-once SideEffect Ledger、durable timer、ExecutionManifest、Context Snapshot、episodic memory、Skill 供应链治理，以及本地 MCP 2025-11-25 / A2A 协议边界；使用 HR 入职到转正的跨天 Case 作为 Reference Application。

## 最值得深挖的技术点

### 1. Event Store、Checkpoint 和 Projection 的职责分离

- LangGraph checkpoint 保存“执行到哪里”，不承担长期业务事实审计。
- append-only Event Store 保存不可变事实、sequence、command id 和 hash chain。
- PostgreSQL Case projection 服务查询/UI，可从事件重建。
- event 与 outbox 同事务提交，projector/publisher 使用 claim + ack 幂等消费。

面试可讨论：为什么 graph state 不能替代业务数据库、event schema 演进、projection lag、outbox stuck recovery、乐观锁冲突处理。

### 2. Effectively-once 而不是虚假的 exactly-once

- 写工具执行前以 `run_id + tool_call_id` 预留 idempotency key。
- ledger 绑定 tool、subject hash 和审批对象。
- 已成功结果直接复用；网络超时导致结果未知时进入 `unknown`，拒绝盲目重试，交给 reconciliation。

面试可讨论：数据库事务无法覆盖外部系统、副作用未知状态、业务幂等键设计。

### 3. 审批是可失效授权，不是一个布尔值

- 审批绑定工具参数、evidence、policy version 和 ExecutionManifest hash。
- 支持 revision、expiry、revoke、supersede 和 admin maker-checker。
- resume 时重新检查 ACL、有效期和 subject hash；批准不等于永久授权。
- Edit 创建新 revision，旧审批保留为 superseded 审计记录。

### 4. 跨天执行与并发推进

- Durable Timer 持久化 SLA、审批过期和试用期提醒。
- scheduler 用 skip-locked/single-claim 防止重复触发。
- run lease 使用 fencing token，过期 owner 的旧写入不能覆盖新 owner。
- SSE 以 Event Store sequence 为 cursor，支持断线续读。

### 5. Evidence Versioning

- `DocumentVersion` 不可变，内容用 SHA-256 标识。
- chunk ID 由文档版本、标题路径、序号和内容稳定生成。
- Dense/BM25/RRF/reranker/citation 全链路传播版本。
- 检索过滤非 active version，执行前 EvidenceFreshnessValidator 拒绝过期引用。

### 6. Context Engineering、Memory 和 Skill

- Context Snapshot 遵循 write/select/compress/isolate，只压缩安全自包含前缀。
- 未决审批、工具失败和 policy decision 被 pin；原始事件永不删除。
- invariant hash 验证 citations、审批和失败原因未在压缩中丢失。
- Episodic Memory 有 tenant ACL、provenance、poisoning quarantine 和 forget。
- Skill 有来源白名单、checksum、eval promotion gate 和撤销生命周期。

### 7. MCP 与 A2A 的合理边界

- MCP 2025-11-25 reference server 暴露 tools/resources/prompts 和 structured output。
- MCP 写工具仍进入 ToolRegistry、ACL、ApprovalManager 和 SideEffect Ledger。
- A2A Policy Research Agent 只读、独立 AgentCard/Task/Artifact；主 Harness 保留计划、审批与写权限。
- A2A 只有在独立部署、权限和维护域时有价值，不把“多 Agent”当装饰。

### 8. Safety Eval 与可观测性

- Safety Eval 扫描真实有序 trajectory，而不是只测最终文本。
- 覆盖 unauthorized retrieval、write without approval、duplicate side effect、missing citation。
- OTel/Phoenix span 关联 `case_id/run_id/event_id`。
- 指标包含 approval wait、outbox backlog、projection lag、protocol success/failure、repair、budget exhaustion 和安全零值。

## 技术与业务映射

| 技术 | 业务必要性 |
| --- | --- |
| Working Memory | 保存当前 Case、待办和未决审批 |
| Episodic Memory | 召回相似 Case 经验并保留来源 |
| Skill | 版本化入职/转正 SOP 并用评测晋级 |
| Context Compression | 跨月事件过多但不能丢失治理事实 |
| MCP | 统一制度资源、人员查询和工单能力 |
| A2A | 把只读制度研究隔离到独立权限域 |
| LangGraph | 计划、执行、中断和 checkpoint/resume |
| Event Store | 跨天审计、回放、Timeline 和 rebuild |
| Durable Timer | SLA、审批过期、提醒和失败重试 |
| Safety Eval | 证明不会越权、绕过审批或重复写入 |

## 标准 Demo 讲法

1. 创建长期 `HRCase`，固定本次执行的 manifest。
2. Harness 加载已激活的 onboarding Skill。
3. A2A Policy Research Agent 返回带版本 citations 的 evidence artifact。
4. Harness 生成 HR/IT/主管计划，准备通过 MCP 创建工单。
5. 写操作触发审批，Case 进入 `waiting_approval`。
6. 第二天批准后，Harness 重新校验审批对象与权限。
7. SideEffect Ledger 保证重复 resume 不会重复创建工单。
8. 系统写入 memory 并调度 80 天后的 durable timer。
9. 控制台用 Artifact Timeline 展示所有不可变事件和指标。

## 工程边界与诚实表述

已实现的是工业模式的可运行 reference implementation，不应夸大为已在大规模生产部署：

- PostgreSQL adapters 与 SQLite deterministic tests 共用 async SQLAlchemy 契约。
- full mode 可接真实 PostgreSQL/Redis/Milvus/Elasticsearch/MinIO/Qwen/Phoenix。
- MCP/A2A 当前为本地 HTTP/JSON 标准形态，未使用官方 SDK 的完整 session/transport。
- 不接真实 HR 系统，不实现完整生产 IAM、Kafka、Temporal、Kubernetes 或 GraphRAG 主链路。

## 高频追问

### 为什么 HR 场景值得做？

场景价值不在 HR 名词，而在它自然逼出制度版本、跨角色、跨天、审批、写副作用、SLA、权限和审计。如果换成普通客服或旅行规划，反而很难合理解释 durable execution、审批失效和 side-effect reconciliation。

### 为什么同时需要 LangGraph checkpoint 和 Event Store？

checkpoint 优化执行恢复，Event Store 保存业务事实和审计。两者保留周期、查询方式和 schema 演进目标不同。仅用 graph state 会让审计、Timeline 和 projection rebuild 依赖编排框架内部结构。

### 为什么不直接上 Temporal？

项目目标是展示 Agent Harness 的领域治理，而不是展示工作流产品配置。当前 Event Store、lease 和 timer 已足够验证核心语义；如进入真实高规模生产，Temporal 可以替换 durable scheduling 层，但审批对象、evidence freshness 和 side-effect ledger 仍需要领域实现。

### 为什么没有 GraphRAG？

主场景的难点是执行治理，不是复杂实体关系推理。混合检索和版本化 evidence 已满足制度查证；加入 GraphRAG 会扩大基础设施，却不能提升审批、恢复和幂等的证明力。

## 面试开场

> 企业内部很多流程不是简单问答，而是由非结构化制度驱动、跨角色和跨天运行，并包含有副作用的系统操作。普通 RAG 只能回答，普通 workflow 又无法理解制度和处理例外。我实现了一个以 RAG 为证据层、以 Agent Harness 为治理层的企业流程 Agent Runtime，并用员工入职到转正的长期 Case 验证审批、恢复、幂等、记忆、协议和安全评测。
